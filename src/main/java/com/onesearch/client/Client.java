package com.onesearch.client;

public class Client {
    public void connectToServer(String serverAddress, int port) {
        // Logic to connect to the server
        System.out.println("Client attempting to connect to server at " + serverAddress + ":" + port);
    }
}
